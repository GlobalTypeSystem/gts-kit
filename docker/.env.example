# Docker Compose Environment Variables
# Copy this file to .env and customize as needed

# Server Configuration
GTS_SERVER_PORT=7806
GTS_SERVER_VERBOSITY=normal
# GTS_SERVER_VERBOSITY options: silent, normal, debug

# Web App Configuration
# The API base URL that the browser will use to connect to the server
# This should typically be http://localhost:7806 when running locally
GTS_SERVER_API_BASE=http://localhost:7806

# Port Mappings
# Web app external port (access via http://localhost:WEB_PORT)
WEB_PORT=7805
# Server external port (access via http://localhost:SERVER_PORT)
SERVER_PORT=7806

# Data Directory
# The server will store data in this directory on the host
# Default: ${HOME}/.gts-viewer/server
# DATA_DIR=/custom/path/to/data
